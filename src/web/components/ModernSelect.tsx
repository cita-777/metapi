/**
 * Backward-compatible import path. Keep this file deliberately tiny so the
 * project-owned `components/ui` layer remains the single implementation.
 */
export {
  default,
  type ModernSelectOption,
  type ModernSelectProps,
} from './ui/Select.js';
