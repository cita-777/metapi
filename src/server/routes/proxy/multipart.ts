/** Compatibility facade for route-local imports; implementation is neutral. */
export {
  cloneFormDataWithOverrides,
  ensureMultipartBufferParser,
  isMultipartRequest,
  parseMultipartFormData,
} from '../../services/multipart.js';
